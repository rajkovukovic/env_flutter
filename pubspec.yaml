name: flutter_env
version: 0.1.0
description: Easily configure any flutter application with global variables using a `.env` files.
homepage: https://github.com/rajkovukovic/flutter_env
environment:
  sdk: ">=2.12.0-0 <3.0.0"
dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
